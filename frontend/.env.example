# Copy to .env.local and fill in. Only VITE_-prefixed vars are exposed to the client.

# Backend REST base (includes /api/v1)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# App metadata
VITE_APP_NAME=DNR Pest Control Admin
VITE_ENV=development
